// import Vue from 'vue';
var Vue = require('../src/vue-2.0.0-beta.1/dist/vue.common');

new Vue({
    el: '#app',
    data: {
        person: {
            first: 'Bob',
            last: 'Smith'
        }
    }
});
